## Iterator

Iterator is used when,
- A collection has to be traversed without exposing it's internal structure
- multiple ways required to traverse a collection
- decouple collection logic and iteration logic

**Real-Life Analogy**
 vending machine -  You don’t need to know how the snacks are arranged inside or where exactly your favorite drink is stored